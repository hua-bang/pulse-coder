## Task
按照画布文档《组件拆分建议（超标组件）》落实组件拆分。目标：单组件 ≤ 300 行、职责单一。不要处理 hooks 文件（hook 暂不关注）。

## Scope
以下组件需要拆分：
1) Sidebar (src/components/Sidebar/index.tsx)
2) AgentNodeBody (src/components/AgentNodeBody/index.tsx)
3) Canvas (src/components/Canvas/index.tsx)
4) ChatPage (src/components/chat/ChatPage.tsx)

## 指导建议（来自画布文档）
### Sidebar
- 拆成：Sidebar.tsx, SidebarHeader.tsx, WorkspaceList.tsx, WorkspaceItem.tsx, FolderItem.tsx, InlineCreateRow.tsx, LayersPanel.tsx, LayerItem.tsx, LayerContextMenu.tsx, utils/layers.ts
- Sidebar.tsx 仅保留 props 映射/核心 state/拼装 sub components

### AgentNodeBody
- 拆成：AgentNodeBody.tsx, AgentPicker.tsx, AgentTerminal.tsx, AgentIcon.tsx, utils/terminal.ts
- AgentNodeBody 仅保留选择状态 + 条件渲染 <AgentPicker/> / <AgentTerminal/>

### Canvas
- 拆成：Canvas.tsx, CanvasSurface.tsx, CanvasOverlays.tsx
- Canvas.tsx 仅保留 hooks 绑定 + 向子组件传参

### ChatPage
- 拆成：ChatPage.tsx, ChatPageBody.tsx（最低成本即可）

## Notes
- 保持原有功能一致，确保导出路径不破坏其他引用（必要时调整 index.ts 重新导出）。
- 遵守项目现有代码风格。
- 拆分后请确保每个文件行数 ≤ 300。

请直接在代码中完成拆分并自检引用路径，必要时更新 import/export。