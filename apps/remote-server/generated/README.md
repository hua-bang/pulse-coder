# generated/

This directory is for local/generated artifacts only (charts, exported pages, temporary analysis outputs).

To keep the repository lightweight, generated outputs are ignored by git.

- Keep only this `README.md` and optional `.gitkeep` in version control.
- Store shareable large outputs in release assets, external storage, or workspace artifacts.
